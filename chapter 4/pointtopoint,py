# pointToPointCommunication.py
from mpi4py import MPI

comm = MPI.COMM_WORLD
rank = comm.Get_rank()

if rank == 0:
    comm.send("Message from process 0", dest=1)
    print("Process 0 sent the message.")
elif rank == 1:
    msg = comm.recv(source=0)
    print(f"Process 1 received: {msg}")
